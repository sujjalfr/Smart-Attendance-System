// QRScanner removed — UI no longer uses QR scanning.
import React from "react";

const QRScanner = () => {
  return (
    <div>
      <h2>QR scanning disabled</h2>
      <p>This project no longer supports QR-based attendance. Use face scan.</p>
    </div>
  );
};

export default QRScanner;